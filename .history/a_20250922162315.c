#include <stdio.h>

char * full_name(char fullName[]){
    return fullName;
}

int main() {
    char * fullName = full_name("Zouhair Folki");
    char * fullName2 = full_name("Younes Bougma")

    printf("%s", fullName);

    return 0;
}